code: ppk
name: "Prototype Parity Kit"
description: "Versioned prototype evidence and visual parity gates for UI delivery"
module_version: 0.5.1
default_selected: false
module_greeting: >
  Prototype Parity Kit is configured. Capture prototype evidence before UI implementation,
  bind each UI story to that evidence, run the story parity gate, then audit the scoped
  routes before closing an epic.
